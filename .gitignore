# ============================================================================
#  .gitignore : Git の追跡対象から外すファイル/フォルダの指定
#  ----------------------------------------------------------------------
#  Visual Studio / MSBuild は「ビルドすれば再生成できる中間物」を大量に
#  吐き出します。これらをリポジトリに含めると、
#    ・容量が肥大化する
#    ・環境依存のパスが混入して他PCで壊れる
#    ・毎回巨大な差分が出てレビューできない
#  ため、すべて除外します。
# ============================================================================

# ---------------------------------------------------------------------------
# Visual Studio のユーザー固有設定 / キャッシュ
# ---------------------------------------------------------------------------
.vs/                    # IntelliSense DB、ウィンドウレイアウト等（個人設定）
*.vcxproj.user          # デバッグ時の作業ディレクトリ等（個人設定）
*.suo
*.userosscache
*.sln.docstates

# ---------------------------------------------------------------------------
# ビルド出力（.obj / .exe / .pdb / .ilk など）
# ---------------------------------------------------------------------------
[Dd]ebug/
[Rr]elease/
[Ww]in32/
[Xx]64/
[Bb]uild/
[Bb]in/
[Oo]bj/
ipch/
*.obj
*.exe
*.pdb
*.ilk
*.idb
*.iobj
*.ipdb
*.tlog
*.log
*.lastbuildstate
*.res
*.pch
*.aps

# ---------------------------------------------------------------------------
# コンパイル済みシェーダー（.hlsl から生成されるため追跡不要）
# ---------------------------------------------------------------------------
*.cso
*.fxc

# ---------------------------------------------------------------------------
# OS が勝手に作るファイル
# ---------------------------------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store

# ---------------------------------------------------------------------------
# VSCode : 共有したい設定（tasks.json 等）は追跡し、個人設定のみ除外
#   → .vscode/ 全体は無視せず、個人用ファイルだけをピンポイントで除外します
# ---------------------------------------------------------------------------
.vscode/ipch/
.vscode/*.db
.vscode/settings.local.json
