//=============================================================================
// Window.h
//   Win32 のウィンドウを 1 枚作り、メッセージ処理を行うクラス。
//
//   ■ このクラスの責務（ここだけを担当する）
//     ・ウィンドウクラスの登録とウィンドウの生成
//     ・OS から届くメッセージ（マウス、キー、サイズ変更、閉じるボタン…）の処理
//     ・「閉じられた」「サイズが変わった」という事実を外へ通知する
//
//   ■ 責務でないもの（意図的に持たせないもの）
//     ・DirectX に関すること一切。
//       ウィンドウは「絵を描く紙」であって「絵の描き方」は知らなくてよい。
//       この分離のおかげで、描画側を差し替えてもウィンドウ側は無変更で済む。
//=============================================================================
#pragma once

#include "../Common/GraphicsCommon.h"

#include <cstdint>
#include <functional>
#include <string>

namespace dx12
{
class Window
{
public:
    //-------------------------------------------------------------------------
    // サイズ変更が起きたときに呼ばれるコールバックの型
    //   引数は新しいクライアント領域（枠を除いた描画可能部分）の幅・高さ。
    //-------------------------------------------------------------------------
    using ResizeCallback = std::function<void(uint32_t width, uint32_t height)>;

    Window() = default;
    ~Window();

    //-------------------------------------------------------------------------
    // コピー・ムーブ禁止
    //   このクラスは HWND という OS リソースを 1 つだけ所有します。
    //   コピーされると同じ HWND を 2 回破棄しようとして壊れるため、
    //   コンパイル時点で禁止しておきます（＝バグの芽を型で潰す）。
    //-------------------------------------------------------------------------
    Window(const Window&) = delete;
    Window& operator=(const Window&) = delete;

    //-------------------------------------------------------------------------
    // ウィンドウを生成して表示する
    //   @param title  タイトルバーに表示する文字列
    //   @param width  クライアント領域の幅（ピクセル）
    //   @param height クライアント領域の高さ（ピクセル）
    //-------------------------------------------------------------------------
    void Create(const std::wstring& title, uint32_t width, uint32_t height);

    //-------------------------------------------------------------------------
    // 溜まっているメッセージをすべて処理する
    //   @return true  … 実行を継続する
    //           false … ウィンドウが閉じられた（アプリを終了すべき）
    //
    //   ゲームやリアルタイム描画では GetMessage（メッセージが来るまで待つ）
    //   ではなく PeekMessage（あれば取る／なければ即座に戻る）を使います。
    //   待たずに戻ることで、入力が無くても毎フレーム描画を続けられます。
    //-------------------------------------------------------------------------
    bool ProcessMessages();

    //-------------------------------------------------------------------------
    // サイズ変更時に呼ばれる処理を登録する
    //-------------------------------------------------------------------------
    void SetResizeCallback(ResizeCallback callback) { m_onResize = std::move(callback); }

    // --- 参照用アクセサ ------------------------------------------------------
    HWND     Handle() const noexcept { return m_hwnd; }
    uint32_t Width()  const noexcept { return m_width; }
    uint32_t Height() const noexcept { return m_height; }

private:
    //-------------------------------------------------------------------------
    // ウィンドウプロシージャ（OS からのメッセージを受け取る関数）
    //
    //   ■ なぜ static なのか
    //     Windows は C 言語時代からの API なので、コールバックに「関数ポインタ」
    //     しか渡せません。C++ の非静的メンバ関数は暗黙の this を必要とするため
    //     関数ポインタに変換できず、static にする必要があります。
    //
    //   ■ ではどうやって this を取り戻すのか
    //     CreateWindowExW の最後の引数に this を渡しておくと、
    //     WM_NCCREATE メッセージの lParam 経由で受け取れます。
    //     それを SetWindowLongPtrW(GWLP_USERDATA) でウィンドウに保存し、
    //     以降のメッセージでは GetWindowLongPtrW で取り出します。
    //     これが Win32 + C++ で定番の「this の運び方」です。
    //-------------------------------------------------------------------------
    static LRESULT CALLBACK StaticWindowProc(HWND hwnd, UINT message, WPARAM wParam, LPARAM lParam);

    //-------------------------------------------------------------------------
    // 実際のメッセージ処理（こちらはインスタンスメンバ）
    //
    //   hwnd をメンバ (m_hwnd) から取らず引数で受け取っているのは、
    //   WM_DESTROY 以降 m_hwnd を nullptr にクリアするためです。
    //   破棄後にも WM_NCDESTROY 等が届くので、そこで DefWindowProcW に
    //   nullptr を渡してしまわないよう、OS から渡された hwnd を使います。
    //-------------------------------------------------------------------------
    LRESULT HandleMessage(HWND hwnd, UINT message, WPARAM wParam, LPARAM lParam);

private:
    HWND      m_hwnd     = nullptr;  // ウィンドウのハンドル（OS が振る識別子）
    HINSTANCE m_instance = nullptr;  // 自プロセスのインスタンスハンドル
    uint32_t  m_width    = 0;        // クライアント領域の幅
    uint32_t  m_height   = 0;        // クライアント領域の高さ
    bool      m_shouldClose = false; // 閉じる要求を受けたか

    ResizeCallback m_onResize;       // サイズ変更通知先（未設定なら何もしない）
};

} // namespace dx12
