//=============================================================================
// LabVS.hlsl
//   習作シェーダーが共通で使う頂点シェーダー。
//
//   どの習作も「画面いっぱいの三角形を 1 枚描くだけ」なので、
//   頂点シェーダーは 1 つで足りる。コンパイルも 1 回で済む。
//=============================================================================
#include "../FullScreen.hlsli"

FullScreenVSOutput VSMain(uint vertexId : SV_VertexID)
{
    return FullScreenVS(vertexId);
}
